import { createContext } from "react";
/**
 * @name Context.js
 * @author Schober Andreas
 * @const Context: create a Context which is needed for the global query string
 */
const Context = createContext({});

export default Context;
